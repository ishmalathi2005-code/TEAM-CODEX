# Backend API URL (Update to match your backend server address)
VITE_API_BASE_URL=http://localhost:5000/api
# Set to 'true' to enable built-in mock server fallback when backend API is unreachable
VITE_USE_MOCK_FALLBACK=true
